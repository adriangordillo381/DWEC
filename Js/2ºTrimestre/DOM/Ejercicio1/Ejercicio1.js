// 1. Selecciona el elemento h1 por su ID y muestra su contenido en consola

function mejecutar() {
  const titulo = document.querySelector("#titulo");
  console.log(titulo); // asi solo muestra la etiqueta con ese id

  const primerIntro = document.querySelector("p.intro");
  alert(primerIntro.textContent); //con textContent accede al contenido de la etiqueta

  const todosIntro = document.querySelector();
}
